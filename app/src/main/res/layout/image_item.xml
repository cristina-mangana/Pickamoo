<?xml version="1.0" encoding="utf-8"?>
<!--This layout shows an item of the images, trailers or recommendations list-->
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center_vertical"
    android:layout_margin="@dimen/smallSeparation"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="@dimen/smallPadding">

    <ImageView
        android:id="@+id/iv_image_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/photos_ct"
        android:scaleType="centerCrop"
        tools:src="@drawable/img_placeholder" />

    <!--Play button only visible when the item is a trailer-->
    <ImageView
        android:id="@+id/play_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/rounded_shadow"
        android:contentDescription="@string/trailer_ct"
        android:padding="@dimen/smallSeparation"
        android:src="@drawable/ic_play_circle"
        android:visibility="gone" />
</android.support.v7.widget.CardView>